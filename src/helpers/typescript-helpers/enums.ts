export enum Gender {
  MALE = "male",
  FEMALE = "female",
}

export enum ReqBodyParts {
  BODY = "body",
  PARAMS = "params",
  QUERY = "query",
}

export enum TaskStatus {
  UNKNOWN = "unknown",
  CONFIRMED = "confirmed",
  CANCELED = "canceled",
}
